name: pub_to_hySpc_pkgs

on:
  schedule:
    # * is a special character in YAML so you have to quote this string
    - cron:  '*/1 * * * *'

jobs:
      - name: Move procedure
        run: |
          mkdir archive
